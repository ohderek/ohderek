# Copy this file to .env and fill in your values.
# .env is in .gitignore — it will never be committed to version control.
#
# cp .env.example .env

# ─── Required ─────────────────────────────────────────────────────────────────
# Your OpenAI API key. Get one at https://platform.openai.com/api-keys
# Cost estimate: ~$0.01 per 100 questions (embeddings + GPT-4o)
OPENAI_API_KEY=sk-...

# ─── Mode ─────────────────────────────────────────────────────────────────────
# true  → local DuckDB database (run `make seed` first, no cloud account needed)
# false → real Snowflake (fill in credentials below)
USE_MOCK_DB=true

# ─── Snowflake (only needed if USE_MOCK_DB=false) ─────────────────────────────
# SNOWFLAKE_ACCOUNT is the part before .snowflakecomputing.com in your URL
# e.g. if your URL is xy12345.us-east-1.snowflakecomputing.com → xy12345.us-east-1
SNOWFLAKE_ACCOUNT=xy12345.us-east-1
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password          # prefer key-pair auth in production (see CREDENTIALS.md)
SNOWFLAKE_DATABASE=ANALYTICS
SNOWFLAKE_SCHEMA=MARTS
SNOWFLAKE_WAREHOUSE=ADHOC__MEDIUM
SNOWFLAKE_ROLE=ANALYST

# ─── Optional Tuning ──────────────────────────────────────────────────────────
# CHAT_MODEL=gpt-4o                 # or gpt-4o-mini to reduce cost
# EMBEDDING_MODEL=text-embedding-3-small
# MAX_ROWS=500                      # maximum rows returned per query
# MAX_TOKENS_RESPONSE=1024          # maximum tokens in the formatted answer
